# 체스 나이트 

# 나이트 기준점이 0,0일 때 움직일 수 있는 경우의 수
# (-1, 2), (1, 2), (-2, 1), (2, 1), (-2, -1), (2, -1), (-1, -2), (1, -2)
# 가로는 1~8, 세로는 a~h 까지
# 입력은 a1, b2 이런 방식으로

li = [(-1, 2), (1, 2), (-2, 1), (2, 1), (-2, -1), (2, -1), (-1, -2), (1, -2)]
x = input()
cnt = 0
r = int(x[1]) # 행
c = ord(x[0]) - ord('a') + 1 # 열

for i in li:
    if 1 <= r + i[1] <= 8 and 1 <= c + i[0] <= 8:
        cnt += 1
print(cnt)
